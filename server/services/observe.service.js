const Observer = require('./observe.class');

/** More functionalities can be added here
 * like extending the service with new methods 
 * or connecting with other services. 
 */
const observerService = new Observer();

module.exports =  observerService;
